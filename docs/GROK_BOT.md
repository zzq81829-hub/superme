# GROK BOT

Status: **EXPERIMENTAL / UNKNOWN_CONTROL_INTERFACE**.

`~/.grok/bin/agent.exe` is Grok Build TUI, not a bot control API.  
Grok Bot is never required. `required: false`. Cost Guard skips it.

Do not equate Grok model, Grok Build, and Grok Bot.
